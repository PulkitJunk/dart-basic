void main(){
  //var
  var str="hello";
  print(str);
  // str=10; // Error: A value of type 'int' can't be assigned to a variable of type 'String'.
  
  // dynamic
  dynamic section;
  section='first';
  print(section);
  section=7;
  print(section);
  section=true;
  print(section);
}